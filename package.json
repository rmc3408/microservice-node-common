{
  "name": "@rmc3408/microservice-node-common",
  "version": "0.0.1",
  "description": "",
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "files": [
    "./build/**/*"
  ],
  "scripts": {
    "clean": "del build",
    "version": "npm version patch",
    "build": "npm run version && npm run clean && tsc",
    "commit": "git add . && git commit -m 'update' && git push",
    "publish": "npm run build && npm run commit && npm publish"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "del-cli": "^5.1.0",
    "typescript": "^5.5.4"
  }
}
